-- LifeDrop Blood Donor System - Schema Initialization

-- Use the database (Spring Boot will handle this if createDatabaseIfNotExist=true)
-- CREATE DATABASE IF NOT EXISTS blood_donor_db;
-- USE blood_donor_db;

-- The tables will be automatically created by Hibernate (spring.jpa.hibernate.ddl-auto=update)
-- But we can provide explicit schema here if needed. 
-- For now, let's just use data.sql for initial data.
